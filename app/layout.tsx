import type { Metadata, Viewport } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import { Providers } from "./providers";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
  display: "swap",
});

export const viewport: Viewport = {
  width: "device-width",
  initialScale: 1,
};

export const metadata: Metadata = {
  title: "Coeus Review & Training Specialist, Inc.",
  description: "Professional review programs for Criminology and Nursing licensure examinations, and CPD seminars for all licensed professionals.",
  keywords: ["review center", "criminology", "nursing", "CPD seminars", "licensure exam", "professional development"],
  authors: [{ name: "Coeus Review & Training Specialist, Inc." }],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="scroll-smooth">
      <body
        className={`${inter.variable} antialiased`}
      >
        <Providers>
          {children}
        </Providers>
      </body>
    </html>
  );
}
